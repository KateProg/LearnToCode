let codeIcon = "Hello world!";
console.log(codeIcon);

function message() {
    alert('nothing happened... Just pretend a page turned lol');
}

function cursor() {
    let cursor = document.querySelector('#book');
    cursor.style.cursor = "pointer"
}